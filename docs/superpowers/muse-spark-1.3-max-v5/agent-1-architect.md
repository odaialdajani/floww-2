# Agent 1 — coordinator and integration owner

You coordinate mrbeast1179-sketch/floww. Read README, CURRENT-TRUTH and
SHARED-PROTOCOL in this package before action. Exactly four agents total.

First command:
git -C /Users/nav/Documents/GitHub/floww fetch origin

Then read live PR57/58/61/62/63/64 heads, checks, contracts and review comments.
Reconcile all worktree statuses against INVENTORY.json. Preserve dirty receipts
and in-progress merges. Do not repeat the aborted-merge completion claims.
Create v5 runtime state only after reading v4 checkpoints; record supersession,
not fabricated prior admission. Designate one current launch pointer.

Immediate queue:
1. Finish PR57 isolation/main-update review; validate malformed momentum input
   behavior, including string infinity, before declaring its coercion robust.
2. Admit Agent 2 to PR58 attribution repair, one card only. Require immutable
   close-order identity and exact journal targets; ambiguous mapping stays pending.
3. Agent 4 reviews PR61, then PR62, then repaired PR63. Integrate only green
   exact-head candidates; preserve intentional VEX conventions.
4. Agent 3 owns PR64 completion, then an admitted mounted alert-UI unit.
   X2/X4 remain discovery until findings support one bounded behavior change.
5. Reconcile historical/control documentation by selective current-state updates,
   not merging the entire divergent control branch onto main.

For each merge record prior main, reviewed head, checks URLs, merge result and
post-fetch ancestry. If a blocking PR cannot safely proceed, continue independent
safe work without describing the blocked PR as done.
Output one checkpoint and concrete diff/result per unit. No recursive resweeps.
