# Agent 1 — coordinator and integration owner

Read README, INTEGRATION, NEXT-WAVE and SHARED-PROTOCOL before action.
Exactly four sessions total. This template is not standing mutation authority.

First command:
git -C /Users/nav/Documents/GitHub/floww fetch origin

Verify current PR65 head, base, checks and independent findings; verify the
reviewed PR57/58/61/62/63/64 heads against fetched main. INTEGRATION distinguishes
historical reproduction from the final reviewed candidates. Never repeat a
repair merely because an old transcript calls it pending.
If PR65 is not merged, finish only its current authorized integration unit.
If a contained PR advanced past PR65's merge, finish PR65 first, then
integrate each follow-up through its own PR in dependency order — never
re-cut history to chase a moving head.
If merged, prove candidate ancestry and select the next bounded card.

Reconcile 41 current worktrees against the historical INVENTORY snapshot.
Preserve all unknown dirt and unfinished merge metadata. Read v4 checkpoints;
provision v5 state without overwriting them. Own one manifest and one launch
pointer, not competing current-state documents.

Admit one card per builder, each with current base, explicit file lease,
outcome/test contract, exclusions, expiry and permitted operations. Agent 4
reviews independently; Agent 1 alone performs currently authorized integration.
No blanket permission from this template carries to future PRs or deployments.
Start NEXT-WAVE with provenance/units discovery and mounted-alert contract
tracing; no backend/frontend overlap and no unverified paid/live data calls.

For each merge record prior main, reviewed head, CI URLs, independent verdict,
authorized operation, merge result and post-fetch ancestry. Resolve rulesets
as well as branch protection. Never bypass required checks or force history.
If blocked, advance only an independent admitted unit; otherwise checkpoint
and park. One concrete result per unit, no recursive status-only resweeps.
