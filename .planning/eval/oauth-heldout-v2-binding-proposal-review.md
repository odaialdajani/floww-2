# Independent input-binding proposal review

Status: PROPOSAL_NOT_RUNNER_READY / NOT_RUN. Prepared from the two new recorded Public source files and the frozen questions/protocol amendment. No application edits, model/provider calls, history seeding, runner construction or mobile work occurred.

All30 literal questions are retained in order. Twenty-nine have concrete input/setup proposals; the AAPL company question remains not_assessable because no AAPL source was captured. It stays in the30-case denominator. An absent file is not evidence that Public lacks AAPL data, and a deterministic no-price fallback is not a model-tested interpretation.

The fixed replay clock is2026-09-11T22:16:12.221466+00:00, the latest captured map time. All four original chain quotes are stale then; all original chain/map source observation times are unknown. Build, receipt and quote times are never substituted for option-source observation times. Original source.snapshot facts/gaps and all answer outputs were excluded as oracles.

Independent expected values use raw field extraction, decimal signed gamma sums, direct map-cell/sum calculations and the exchange calendar. Concrete checks include SPY's September14 cell at764 with value48413690.34976003, QQQ's full-map flip693.95 spanning six expiries, the September14-18 five-session window, and the exact previous session close2026-09-10T20:00:00+00:00. Source chains contain only two expiries each; broader six-expiry map coverage cannot fill missing raw-chain scope.

Three clearly labeled synthetic recipes are proposed, not executed: an exact-version map cache miss; an IWM history coverage mismatch assembled from two genuine recorded quote times and a real contract subset; and a stale SPY map with an explicitly artificial source time ten minutes before the replay clock. Raw artifacts stay unchanged. The stale-map recipe isolates source-age handling and labels its derived source synthetic; it never claims Public supplied that timestamp.

Still required before either arm: implement the isolated recorded-input/clock replay route, materialize/hash transformed copies, freeze equivalent owner/history states and screen-change event order, generate/check bounded fact ledgers against these independent expectations, pin candidate settings/versions, and record real dispatch/usage/save evidence. The unrecorded alert store must not be described as inspected or empty; explicitly bind a controlled no-supplied-alert state or acquire genuine alert evidence.

The52-call allowance question is pending and supplies no authorization. No quota reset, allowance increase or comparison is permitted by this proposal. The original40-call limit and last reported nine remaining calls stay binding until the user answers. The AAPL gap and setup gaps independently prevent claiming complete acceptance.

Offline verification passed JSON parsing,30-row count, unchanged literal question order, exact map-selected expiry scope, and SHA256 checks for every original source/protocol file. The proposal includes all input hashes. Parent added Git byte-preservation rules; verify checkout bytes against those hashes before execution. No frozen original was changed.
